import {domains,type Lang} from '../cognitive/types';
import {names} from './i18n';
export function Neural({lang,scores}:{lang:Lang;scores?:number[]}){return <svg className="neural" viewBox="0 0 700 560" role="img" aria-label={lang==='fr'?'Carte des six domaines cognitifs':'Map of six cognitive domains'}><defs><radialGradient id="halo"><stop stopColor="#00c8ff" stopOpacity=".18"/><stop offset="1" stopColor="#00c8ff" stopOpacity="0"/></radialGradient></defs><circle cx="350" cy="280" r="235" fill="url(#halo)"/>{[100,170,230].map(r=><circle key={r} className="orbit" cx="350" cy="280" r={r}/>)}{domains.map((d,k)=>{const angle=k*Math.PI/3-Math.PI/2;const x=350+210*Math.cos(angle),y=280+210*Math.sin(angle);return <g key={d}><path className="synapse" d={`M350 280 Q${350+Math.sin(angle)*110} ${280+Math.cos(angle)*110} ${x} ${y}`}/><circle className="node-halo" cx={x} cy={y} r="22"/><circle className="node" cx={x} cy={y} r="6"/><text x={x} y={y+(k===0?-28:38)} textAnchor="middle">{names[lang][d].toUpperCase()}{scores?` · ${scores[k]}`:''}</text></g>})}<circle cx="350" cy="280" r="61" className="core"/><text className="core-text" x="350" y="288" textAnchor="middle">{scores?'Σ':'S'}</text><text x="350" y="375" textAnchor="middle" className="micro">COGNITIVE NETWORK</text></svg>}
