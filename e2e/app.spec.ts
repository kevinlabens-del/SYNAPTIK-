import {test,expect} from '@playwright/test';
const sizes=[[360,640],[390,844],[412,915],[768,1024],[1366,768],[1920,1080]];
for(const [width,height] of sizes)test(`layout ${width}x${height}`,async({page})=>{await page.setViewportSize({width,height});await page.goto('/');await expect(page.getByRole('button',{name:/BEGIN ANALYSIS/})).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true);await page.screenshot({path:`test-results/home-${width}.png`,fullPage:true});await page.getByRole('button',{name:/BEGIN ANALYSIS/}).click();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true)});
test('complete Quick assessment, restore history, practice and offline reload',async({page,context})=>{const errors:string[]=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('/');await page.getByRole('button',{name:/BEGIN ANALYSIS/}).click();await page.getByRole('button',{name:/quick/i}).click();await page.getByRole('button',{name:/3 exercices/}).click();for(let n=0;n<3;n++){await page.locator('.option').first().waitFor();await page.locator('.option').first().click();await page.getByRole('button',{name:/Valider ma réponse/}).click();await page.getByRole('button',{name:/Exercice suivant/}).click()}for(let n=0;n<36;n++){await page.locator('.option').first().waitFor();const first=page.locator('.option').first();if(await first.isEnabled())await first.click();await page.getByRole('button',{name:/Valider ma réponse/}).click();if(n===2){await page.getByRole('button',{name:/Pause et quitter/}).click();await page.reload();await page.getByRole('button',{name:/Reprendre mon évaluation/}).click()}}await expect(page.getByText('Votre empreinte cognitive')).toBeVisible();await expect(page.locator('.profile-list article')).toHaveCount(6);await page.screenshot({path:'test-results/results.png',fullPage:true});await page.getByRole('button',{name:'Mes résultats',exact:true}).click();await expect(page.locator('.history-row')).toHaveCount(1);await page.getByRole('button',{name:'Entraînement',exact:true}).click();await page.locator('.option').first().click();await page.getByRole('button',{name:/Valider ma réponse/}).click();await expect(page.locator('.feedback')).toBeVisible();await page.evaluate(()=>navigator.serviceWorker.ready);await context.setOffline(true);await page.reload();await expect(page.getByRole('button',{name:/BEGIN ANALYSIS/})).toBeVisible();await page.getByRole('button',{name:'Mes résultats',exact:true}).click();await expect(page.locator('.history-row')).toHaveCount(1);expect(errors).toEqual([])});
